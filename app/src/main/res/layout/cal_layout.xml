<?xml version="1.0" encoding="utf-8"?>
<com.applandeo.materialcalendarview.CalendarView android:id="@+id/calendarView"
    android:layout_width="0dp"
    android:layout_height="0dp"
    app:headerColor="@color/darkbgblue"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintLeft_toLeftOf="parent"
    android:clickable="true"

    app:selectedDayBackgroundColor="@color/colorPrimary"
    app:selectedDayTextColor="@color/white"
    app:selectionColor="@color/colorPrimary"
    app:type="many_days_picker"
    android:divider="@android:color/darker_gray"
    android:showDividers="middle"
    app:layout_constraintRight_toRightOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android" />
